const express = require('express')
// const { postBrand, getBrands, getBrand, putBrand, deleteBrand } = require('../controllers/brandControllers')
const router = express.Router()

// router.post('/brand', postBrand)
// router.get('/brand', getBrands)
// router.get('/brand/:brand_id', getBrand)
// router.put('/brand/:brand_id', putBrand)
// router.delete('/brand/:brand_id', deleteBrand)


module.exports = router